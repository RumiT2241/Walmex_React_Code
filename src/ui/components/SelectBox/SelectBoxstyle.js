import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  button: {
    background: "#0072ffd1",
    color: "#fff",
    "&:hover": {
      background: "#0072ffd1",
    },
  },
}));